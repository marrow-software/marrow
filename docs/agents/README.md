# Agent skill config (not published)

These files configure engineering agent skills (`to-tickets`, `triage`, `wayfinder`, etc.).

They are **not** Starlight content. The docs site only loads pages from `src/content/docs/`; nothing under `docs/agents/` is built or deployed to docs.marrow.so.
